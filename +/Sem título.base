views:
  - type: table
    name: Tabela
    groupBy:
      property: file.folder
      direction: ASC
    order:
      - file.name
      - file.folder
    sort:
      - property: file.name
        direction: DESC
      - property: file.folder
        direction: DESC
